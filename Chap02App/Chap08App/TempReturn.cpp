#include <stdio.h>

template <typename T>
void Swap(T& a, T& b) {
	T t;
	t = a; a = b; b = t;
}
template <typename T>
T add

int main() {
	int a = max(1, 2);
	double b = max(1.1, 2.2);
	double c = max(double(2), 3.14);

	int d = max(2, int(3.14));
	printf("d = %d\n", d);
}